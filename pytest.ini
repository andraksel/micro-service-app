[pytest]
testpaths = tests
markers =
    api: service-level API tests
    integration: cross-service integration tests
    contract: service contract tests
    async_events: RabbitMQ/eventual consistency tests
    cache: Redis cache tests
    resilience: failure/timeout tests
    e2e: end-to-end business flows
addopts = -ra
